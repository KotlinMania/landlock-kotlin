# Port targets — `landlock-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `landlock` that codex-rs uses, and therefore the minimum subset that `landlock-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-73] (rank `87`, tier `direct`, status `To Do`)

**Cargo package name**: `landlock`
**Rust module name (in `use` statements)**: `landlock`

## Summary

- **9** distinct symbols imported by codex-rs
- **9** total `use` statements
- **2** codex source files affected
- **2** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `landlock::spawn_command_under_linux_sandbox`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core/src/lib.rs:46` (`codex-core`) — `use landlock::spawn_command_under_linux_sandbox`

### `landlock::ABI`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:14` (`codex-linux-sandbox`) — `use landlock::ABI`

### `landlock::Access`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:17` (`codex-linux-sandbox`) — `use landlock::Access`

### `landlock::AccessFs`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:18` (`codex-linux-sandbox`) — `use landlock::AccessFs`

### `landlock::CompatLevel`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:19` (`codex-linux-sandbox`) — `use landlock::CompatLevel`

### `landlock::Compatible`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:20` (`codex-linux-sandbox`) — `use landlock::Compatible`

### `landlock::Ruleset`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:21` (`codex-linux-sandbox`) — `use landlock::Ruleset`

### `landlock::RulesetAttr`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:22` (`codex-linux-sandbox`) — `use landlock::RulesetAttr`

### `landlock::RulesetCreatedAttr`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-linux-sandbox`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `linux-sandbox/src/landlock.rs:23` (`codex-linux-sandbox`) — `use landlock::RulesetCreatedAttr`
