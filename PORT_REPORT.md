# Port report — `landlock-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `landlock`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 9 `use` sites from 1 other repos
- **Outbound**: 19 `use` sites to 5 other repos
- **Unresolved external**: 1 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `landlock` | `landlock` | 9 | 5 | COD-73 | 87 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (9 edges, 2 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-linux-sandbox` | 8 | ✅ |
| `codex-kotlin` | `codex-core` | 1 | ✅ |

### Symbol-level demand (9 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `landlock::ABI` | 1 | 1 | 0 | 1 |
| `landlock::Access` | 1 | 1 | 0 | 1 |
| `landlock::AccessFs` | 1 | 1 | 0 | 1 |
| `landlock::CompatLevel` | 1 | 1 | 0 | 1 |
| `landlock::Compatible` | 1 | 1 | 0 | 1 |
| `landlock::Ruleset` | 1 | 1 | 0 | 1 |
| `landlock::RulesetAttr` | 1 | 1 | 0 | 1 |
| `landlock::RulesetCreatedAttr` | 1 | 1 | 0 | 1 |
| `landlock::spawn_command_under_linux_sandbox` | 1 | 1 | 0 | 1 |

#### Per-symbol detail (top 9 symbols)

##### `landlock::ABI` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:14` — `use landlock::ABI`

##### `landlock::Access` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:17` — `use landlock::Access`

##### `landlock::AccessFs` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:18` — `use landlock::AccessFs`

##### `landlock::CompatLevel` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:19` — `use landlock::CompatLevel`

##### `landlock::Compatible` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:20` — `use landlock::Compatible`

##### `landlock::Ruleset` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:21` — `use landlock::Ruleset`

##### `landlock::RulesetAttr` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:22` — `use landlock::RulesetAttr`

##### `landlock::RulesetCreatedAttr` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:23` — `use landlock::RulesetCreatedAttr`

##### `landlock::spawn_command_under_linux_sandbox` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `src/lib.rs:46` — `pub_use landlock::spawn_command_under_linux_sandbox`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `libc-kotlin` | 9 | 8 | `libc` | COD-75 | 3 | To Do |
| `thiserror-kotlin` | 1 | 1 | `thiserror` | COD-153 | 11 | To Do |
| `anyhow-kotlin` | 3 | 3 | `anyhow` | COD-10 | 47 | To Do |
| `strum-macros-kotlin` | 2 | 2 | `strum_macros` | COD-143 | 69 | To Do |
| `strum-kotlin` | 4 | 2 | `strum` | COD-142 | 82 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `enumflags2` | 10 | 4 | `landlock` |
