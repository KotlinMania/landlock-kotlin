# Rust callers — `landlock-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `landlock` | `landlock` | 9 | 2 | 1 | 9 |

## `landlock`

### Inbound symbols (9 distinct, 9 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `landlock`.

#### `landlock::ABI` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:14` — `use landlock::ABI`

#### `landlock::Access` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:17` — `use landlock::Access`

#### `landlock::AccessFs` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:18` — `use landlock::AccessFs`

#### `landlock::CompatLevel` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:19` — `use landlock::CompatLevel`

#### `landlock::Compatible` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:20` — `use landlock::Compatible`

#### `landlock::Ruleset` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:21` — `use landlock::Ruleset`

#### `landlock::RulesetAttr` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:22` — `use landlock::RulesetAttr`

#### `landlock::RulesetCreatedAttr` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-linux-sandbox` `src/landlock.rs:23` — `use landlock::RulesetCreatedAttr`

#### `landlock::spawn_command_under_linux_sandbox` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `src/lib.rs:46` — `pub_use landlock::spawn_command_under_linux_sandbox`
